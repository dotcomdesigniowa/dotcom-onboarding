# [EmployeeName] OS

This folder is [Employee Name]'s personal AI operating system at Dotcom Design.
It's plain folders and markdown, readable by any AI tool.

Start with `CLAUDE.md` — that's the master instructions file. Read it first,
every session, before doing anything. It loads the company context and personal
context automatically.

`LESSONS.md` is the self-correcting learning file — read it too.

`Company/` holds synced company context (ICP, North Star, Brand Voice, Offers,
Team, Values, AI Philosophy). Read-only — Josh updates those.

`Context/` holds your personal working style and preferences. Edit freely.

`Agents/` has pre-installed helpers: Ava (build agents), Craig (council).

`Skills/` starts empty and fills as you work.
